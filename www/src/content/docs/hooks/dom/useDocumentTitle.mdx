---
title: useDocumentTitle
description: 'Dynamically update the document title of a webpage.'
subtitle: 'Dynamically update the document title of a webpage.'
---

import { DemoWrapper } from '@/components/demo/wrapper';

import Example from '@/components/demo/useDocumentTitle';

The `useDocumentTitle` hook is helpful when you want to dynamically update the document title based on a state.

<DemoWrapper>
	<Example client:load />
</DemoWrapper>

## Usage

```tsx title="./src/App.tsx"
function App() {
	const [count, setCount] = React.useState(0);

	return (
		<div>
			<p>Count is : {count}</p>
			<button onClick={() => setCount((prev) => prev + 1)}>Increment by 1</button>
		</div>
	);
}

export default App;
```

## API Reference

### Parameters

| Parameter | Type           | Description                       | Default     |
| --------- | -------------- | --------------------------------- | ----------- |
| title     | `string`       | The title to set for the document | N/A         |
| options   | `T` (optional) | The configuration options         | `undefined` |

## Types Reference

### `T`: options

| Parameter      | Type      | Description                                          | Default |
| -------------- | --------- | ---------------------------------------------------- | ------- |
| resetOnUnmount | `boolean` | Whether to reset to the original title after unmount | N/A     |
